# `replicad-evaluator`

Portable code evaluation and shape building utilities for `replicad`.

This package extracts the studio worker's evaluator logic into a reusable API
that can run in browser and Node.js environments.
